# Changelog

本文件记录面向用户的改动。格式大致遵循 [Keep a Changelog](https://keepachangelog.com/)，版本号遵循语义化版本。

## [2.3.0]

### 新增

- 监察者插件：一个专门巡检其他 Claude Code 会话的会话——判断哪个卡住了、哪个的
  问题能代答，通过既有的推送通道汇报，展示面板给出创建入口和巡检记录时间线。
- Jira 同步支持增量：日常只问变化过的那几条工单，不用每次全量扫一遍；设置页配
  了一个通用「插件动作」机制，接上 Jira 的「完整同步」按钮。
- 工单卡片头部新增状态灯带：一颗按 Jira 状态归类的阶段灯，加 PR/检查健康度灯，
  一眼扫过卡片就知道大致进度和 PR/CI 状态，不用点开任何一格 chip。
- 工单列表新增排序：按创建时间、状态、分配，各带正序/倒序。
- 工单卡片加「创建于」维度。
- 失败的 PR 检查行加「发给会话」按钮，能把修复提示直接发进对应会话。

### 变更

- 会话列表的状态判断改用会话记录里的 turn 字段，屏幕上是否停在提示符只作退路。
- 去掉了工单往 PR 写评论的写回路径，只保留把状态转回 Jira 那一半。
- Jira 同步不再被翻页或旧上限悄悄截断在一两百条。

### 修复

- 进程重启后，增量同步一直问不到东西，工单列表缓存永远焐不热。
- 掉出当前 JQL 范围的工单，点刷新也救不回它的状态。

[2.3.0]: https://github.com/niletry/tmux-next/compare/v2.2.0...v2.3.0

## [2.2.0]

### 新增

- 单的进度状态机：跟着 Jira/PR 的状态自动往前推，配套的写回层会把状态迁移同步回
  Jira、把结果评论回 PR，状态变化会推送通知。
- 工单卡片的检查明细按 PR 分组显示，分组标题带 PR 编号、可折叠、旁边有链到原始
  PR 的入口；分组的「项目」用仓库真名字而不是 PR 地址里的 UUID。

### 变更

- 单列表的「表格」视图重新设计成「列表」：一行一张单，状态（标题、chip、会话）
  在左，操作（新会话/刷新/更多）在右，不再需要横向滚动——原来的表格窗口拖窄时
  要横向滚动才能看全，现在换成状态一侧自动换行。已加的筛选字段（如状态、负责人）
  的 chip 会自带维度名，因为不再有表格列头帮着说明这是哪个字段。

### 修复

- 新建会话页在某些情况下没有跟上主题，宽屏下内容被拉伸。
- 检查明细分组标题点了不折叠的问题（`display:flex` 盖过了 `[hidden]`）。

[2.2.0]: https://github.com/niletry/tmux-next/compare/v2.1.1...v2.2.0
